# Editor / tool scratch
.qodo/
.vscode/

# Artifact of a hook writing to a mangled Windows path. Not part of the project.
C:Usersmarkc*

# OS noise
Thumbs.db
.DS_Store

# NOTE: index.html is generated by build.mjs but is deliberately COMMITTED.
# It is the deployable artifact — GitHub Pages serves it directly, and it is
# the whole application in one file. Rebuild with `node build.mjs` after any
# change under src/, and commit the result alongside the source change.

# Hook artifact: a Windows path written as a literal filename.
*claude-notify-signals*

# Probe/harness pages generated during verification. Never part of the repo.
_t*.html
_probe*.html
_sa.html
